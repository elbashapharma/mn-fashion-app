name: mn_fashion_app
description: Order & profit management app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Images & files
  image_picker: ^1.0.7
  path_provider: ^2.1.2

  # Share & formatting
  share_plus: ^7.2.2
  intl: ^0.19.0

  # PDF
  pdf: ^3.10.7
  printing: ^5.12.0

  # ✅ Arabic PDF support (THE ONLY VERSION THAT RESOLVES HERE)
  arabic_reshaper: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
